# AccessTransformer — opens Vanilla Minecraft members used by tfc_aeronautics.
#
# Mirror of the relevant lines from Create's AT. Each entry is needed because
# Parchment 1.21.1 hides the field/method, but our verbatim-chain-conveyor code
# expects to touch it directly (see ServerboundChainConveyorRidingPacket).
#
# Format: <visibility> <class> <member>

# Reset the "fall-damage accumulator" counters on the server-side packet listener
# while a player is riding the chain conveyor. Same AT lines as Create.
public net.minecraft.server.network.ServerGamePacketListenerImpl aboveGroundTickCount
public net.minecraft.server.network.ServerGamePacketListenerImpl aboveGroundVehicleTickCount